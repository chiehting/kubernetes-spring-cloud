server:
  port: 80

spring:
  application:
     name: config-service
  cloud:
    config:
      server:
        git:
          uri: file://opt/src/git-repo
          username:
          password:
      label: master

